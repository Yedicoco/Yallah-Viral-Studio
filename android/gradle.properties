org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=true
android.useAndroidX=false
android.nonTransitiveRClass=true
android.defaults.buildfeatures.resvalues=true

# Le pipeline CI (.github/workflows/android-apk.yml) vérifie la configuration
# via scripts/check-android.mjs puis assemble l'APK de débogage installable.
# org.gradle.daemon=false ci-dessus convient aux agents CI éphémères.
